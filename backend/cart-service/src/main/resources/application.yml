server:
  port: 8082

spring:
  application:
    name: cart-service
  datasource:
    url: jdbc:mysql://localhost:3306/cart_service?createDatabaseIfNotExist=true
    username: root
    password: ASDF1234
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

app:                          # ✅ root level
  internal:
    secret: 12345
book:   # ✅ FIX: moved outside
  service:
    url: http://localhost:8081
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true      # ✅ add this
    ip-address: 127.0.0.1        # ✅ add this
    hostname: localhost

